import { describe, it, expect } from 'vitest';

describe('@skill-quest/shared types (phase 1)', () => {
  it('placeholder — domain types removed until portal phase', () => {
    expect(true).toBe(true);
  });
});
